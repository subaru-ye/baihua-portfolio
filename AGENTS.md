# 个人作品集网站开发指令

## 项目概述
这是一个使用 React + TypeScript + Tailwind CSS 开发的个人作品集网站，主要用于展示个人简介、技能、项目作品和联系方式。

## 技术栈
- React
- TypeScript
- Vite
- Tailwind CSS
- React Router（仅在需要多页面时使用）
- Framer Motion（用于轻量动画）

## 开发目标
- 保持页面简洁、专业、易读。
- 优先完成首页、关于我、作品展示、联系方式四个核心模块。
- 代码结构清晰，方便后续继续扩展博客、深色模式和多语言功能。

## 开发规范
- 使用函数式组件 + Hooks。
- 使用 TypeScript 编写组件和数据结构。
- 使用 Tailwind CSS 编写样式，不要混入大量内联样式。
- 组件要尽量可复用，避免重复代码。
- 数据统一放在 `src/data/` 中管理。
- 组件命名使用 PascalCase。
- 变量和函数命名语义化，避免缩写过多。
- 在有必要的地方添加简洁注释，重点说明复杂逻辑，不写无意义注释。

## 项目结构约定

```text
src/
  components/
    Header.tsx
    Hero.tsx
    About.tsx
    Projects.tsx
    Contact.tsx
    Footer.tsx
  data/
    projects.ts
    skills.ts
    profile.ts
  assets/
    images/
  App.tsx
  main.tsx
```

## 数据管理规范
- 项目数据和技能数据存储在 TypeScript 文件中。
- 使用数组和对象管理内容，方便后续添加和修改。
- 项目数据至少包含：标题、简介、技术栈、分类、链接。
- 不要把大量静态文案直接写死在组件内部，优先抽到 `src/data/`。

## 设计要求
- 默认使用深色主题。
- 背景主色：`#0a0a0a`
- 文字主色：`#ffffff`
- 使用渐变色作为强调色，但不要过度使用。
- 页面视觉风格要简洁、现代、有技术感。
- 保持足够留白，不要堆砌过多装饰元素。

## 交互与动画要求
- 添加平滑滚动效果。
- 使用 Framer Motion 做轻量级进入动画和滚动动画。
- 动画应自然克制，不影响性能和阅读。
- 所有按钮、导航和外部链接都必须可点击、可访问。

## 响应式要求
- 必须适配手机、平板、桌面端。
- 移动端优先保证导航、项目卡片和联系表单的可用性。
- 避免小屏幕出现文字过小、按钮过密、内容溢出的问题。

## 性能要求
- 图片尽量压缩，优先使用懒加载。
- 避免引入不必要的大型依赖。
- 减少无意义动画和重复渲染。

## 代码输出要求
- 优先输出完整可运行代码。
- 修改现有代码时，尽量保持原有结构一致。
- 如果新增组件或数据文件，要与当前目录结构保持一致。
- 不要引入数据库、后台系统或复杂服务端逻辑，除非明确要求。

## 注意事项
- 保持设计简洁，不要过度设计。
- 保持技术方案轻量，优先完成核心展示功能。
- 确保所有链接、按钮、表单交互可正常使用。
- 任何新增功能都应优先考虑是否符合个人作品集网站场景。
